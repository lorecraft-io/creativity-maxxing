.claude-flow/
.ruflo/
.claude/
.mcp.json
CLAUDE.md
.DS_Store

# Auto-generated folder-index note (created by some editors when cloning
# into a parent directory that names its index files after subfolders).
creativity-maxxing.md

# Environment / secrets
.env
.env.*
.env.local
.env.production
.env.development

# Private keys and certificates
*.pem
*.key
*.cert
*.p12
*.pfx
credentials.json

# Node (defensive — step scripts may install deps)
node_modules/

# Media scratch (Remotion renders, whisper audio cache, yt-dlp downloads)
out/
renders/
*.mp4
*.mov
*.webm
*.mp3
*.wav
*.m4a
whisper-cache/
